{
  "name": "tastify",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean",
    "vanilla": "pnpm --filter @tastify/vanilla-showcase exec vite",
    "landing": "pnpm --filter tastify-landing exec astro dev"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  },
  "devDependencies": {
    "turbo": "^2.8.0",
    "typescript": "^5.9.0"
  }
}
